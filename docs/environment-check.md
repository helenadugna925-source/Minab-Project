# Environment Check

- Node.js:
- NPM:
- Go:
- Docker:
- Docker Compose:
- Backend: set `PORT` to change the backend listen port (defaults to `8082`). If the default is in use, the server will automatically fall back to a free port.

Checked on:
OS:
